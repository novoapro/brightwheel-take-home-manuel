# macOS
.DS_Store

# dependencies
/node_modules
/.pnp
.pnp.*

# next.js / build
/.next/
/out/
/build
dist/
*.tsbuildinfo
next-env.d.ts

# testing
/coverage

# logs
*.log
npm-debug.log*

# env / secrets
.env
.env.*
!.env.example
*.pem

# local SQLite data (the app's DB lives on the Railway volume in prod)
/data/
*.sqlite
*.sqlite3
*.db

# Turn-in doc (HTML source + generated PDF): embeds the demo admin passcode —
# keep both local, don't track them in the repo.
/docs/Front-Desk-Documentation.html
/docs/Front-Desk-Documentation.pdf
